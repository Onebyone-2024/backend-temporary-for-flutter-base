version: "3.8"

services:
  app:
    image: ghcr.io/onebyone-2024/backend-temporary-for-flutter-base:sha-16c1b80
    container_name: live-tracking-be
    platform: linux/amd64
    ports:
      - "5556:3000"
    env_file:
      - .env
    restart: unless-stopped
